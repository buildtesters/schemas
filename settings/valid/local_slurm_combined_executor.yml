editor: vi
executors:
  default:
    type: local
    description: submit jobs on local machine
  slurm-executor:
    type: slurm
    description: submit jobs on local machine
    launcher: sbatch
    options:
      - "-p admin"
      - "-t 01:00"